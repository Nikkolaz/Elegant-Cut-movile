import 'package:flutter/material.dart';

class FavoriteBarbersTab extends StatelessWidget {
  const FavoriteBarbersTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Barberos Favoritos'),
    );
  }
}
